[project]
name = "spanish-golang-guide"
version = "0.1.0"
description = "Herramientas de documentación (MkDocs) para la guía de Go en español"
requires-python = ">=3.11"
dependencies = [
    "mkdocs-material>=9.6",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
]
